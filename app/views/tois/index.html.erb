<h1>業種リスト　tois(tipes of industry)</h1>
<!-- <h1>Listing hoiks</h1> -->

<table>
  <tr>
    <th>toi_no</th>
    <th>grp_name</th>
    <th>toi_name</th>
    <th>grp_name</th>
		<th></th>
		<th></th>
		<th></th>
  </tr>

<% @tois.each do |toi| %>
  <tr>
    <td><%= toi.toi_no %></td>
    <td><%= Grp.find(toi.grp_no).grp_name %></td>
    <td><%= toi.toi_name %></td>
    <td><%= link_to 'Show', toi %></td>
    <td><%= link_to 'Edit', edit_toi_path(toi) %></td>
    <td><%= link_to 'Destroy', toi, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Toi', new_toi_path %>
